#include "main.h"

/**
 * _isdigit - checks if c is a digit between 0 - 9
 *
 * @c: holds the value
 *
 * Return: 1 on success
 * On error: return 0
 */
int _isdigit(int c)
{
	if (c <= 9)
	return (1);
	else
	{
		return (0);
	}
}
