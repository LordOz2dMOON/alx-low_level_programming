#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * Return - absolute value of integer
 *
 * @n - collects integer
 *
 */
int _abs(int n)
{
	(if n < 0)
	n = -n;
	return (n);
}
