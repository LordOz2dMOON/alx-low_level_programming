#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 *
 * @n - collects and holds integer
 *
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n < 0)
	n = -n;
	return (n);
}
