#include "main.h"

/**
 * main - Contains a function that prints small letters of the alphabet
 *
 * Return: Always 0: (Success)
 */
int main(void)
{
	void print_alphabet(void)
	{
		char l;

		for (l = 'a'; l <= 'z'; l++)
		{
			_putchar('l');
		}
		_putchar('\n');
	}
	return (0);
}
