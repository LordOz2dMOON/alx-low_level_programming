#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s : holds a pointer to a char s
 * Return: an integer denoting string len
 */
int _strlen(char *s)
{
	return (strlen(s));
}
